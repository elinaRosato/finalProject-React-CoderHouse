import React from 'react';
import StyledLogo from '../styled/Logo.styled';

const Logo = ({size}) => {
  return (
	<div>
    <StyledLogo size={size}>
			Porridge
		</StyledLogo>
  </div>
	)
};

export default Logo;
